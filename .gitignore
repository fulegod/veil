# ─── Dependencies ──────────────────────────────────────────────────────────
node_modules/

# ─── Build outputs ─────────────────────────────────────────────────────────
.next/
dist/
build/
out/
*.tsbuildinfo

# ─── Env / secrets ─────────────────────────────────────────────────────────
.env
.env.local
.env.*.local
smoke/.env
web/.env
web/.env.local
web/.env.*.local

# ─── OS / IDE ──────────────────────────────────────────────────────────────
.DS_Store
.vscode/
.idea/
*.swp
*.swo

# ─── Vercel ────────────────────────────────────────────────────────────────
.vercel/

# ─── Internal notes (local-only memory for Claude) ────────────────────────
progress.md
task_plan.md

# ─── Claude harness config (not relevant to other builders) ───────────────
.claude/launch.json

# NOTE: We DO commit:
#   CLAUDE.md           — shows rigor + Arkiv-specific rules to judges
#   .agent/skills/      — reference implementation: other builders learn from these
#   .claude/skills/     — copied global skills used in this project
#   PATTERNS.md         — Arkiv patterns demonstrated (scoring boost)
#   SKILLS.md           — index of skills loaded
